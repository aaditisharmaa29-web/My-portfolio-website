---
import { APPS } from "../data/apps";
---

<div id="desktop" class="mac-screen">
  <div class="menu-bar">
    <div class="menu-bar-left">
      <span class="menu-item">File</span>
      <span class="menu-item">Edit</span>
      <span class="menu-item">View</span>
      <span class="menu-item">Label</span>
      <span class="menu-item">Special</span>
    </div>
    <div class="menu-bar-right">
      <span id="menu-clock">9:57:05 pm</span>
    </div>
  </div>

  <div id="window-layer" class="window-layer"></div>

  <div class="desktop-icons">
    {
      APPS.map((item) => (
        <button class="desktop-icon" data-app={item.app} data-url={item.url}>
          <span class={`folder-icon ${item.folder}`} aria-hidden="true" />
          <span class="icon-label">{item.title}</span>
        </button>
      ))
    }
  </div>
</div>
